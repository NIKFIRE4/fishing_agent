"""
Конфигурация бота Camp Place
"""
import os
from typing import List

"""
Конфигурация бота Camp Place
"""
import os
from typing import List

# Токен бота (получить у @BotFather)
BOT_TOKEN = os.getenv("BOT_TOKEN", "")

# ID канала для публикации постов
CHANNEL_ID = os.getenv("CHANNEL_ID", "")

# ID администратора, который может модерировать посты
ADMIN_ID = os.getenv("ADMIN_ID", "")

DB_HOST = os.getenv("DB_HOST", "localhost")
DB_PORT = os.getenv("DB_PORT", "5432")
DB_NAME = os.getenv("DB_NAME", "camp_place_bot")
DB_USER = os.getenv("DB_USER", "postgres")
DB_PASSWORD = os.getenv("DB_PASSWORD", "password")

# URL подключения к базе данных
DATABASE_URL = f"postgresql+asyncpg://{DB_USER}:{DB_PASSWORD}@{DB_HOST}:{DB_PORT}/{DB_NAME}"

# Настройки постов
MAX_PHOTOS = 10
MIN_DESCRIPTION_LENGTH = 10
MAX_DESCRIPTION_LENGTH = 1000

# Сообщения
MESSAGES = {
    "welcome": (
        "🏕️ Привет! Рады видеть тебя в боте канала Camp Place ❤️\n\n"
        "Здесь можно поделиться своим походным отчётом или историей — и мы с удовольствием опубликуем её в канале.\n"
        "Спасибо, что делишься опытом 🙏 Это помогает другим путешественникам находить классные места и делает туризм удобнее и интереснее.\n\n"
        "Используй меню ниже, чтобы начать ⬇️"
    ),
    "post_created": (
        "✅ Ура! Твой пост сохранён и уже отправлен на модерацию ✨\n"
        "Совсем скоро админы посмотрят его, и ты получишь уведомление о решении.\n"
        "Спасибо, что делаешь наш туризм лучше! 🏕️"
    ),
    "post_approved": (
        "🎉 Отличные новости! Твой пост одобрен и уже опубликован в канале Camp Place 🌲🔥\n"
        "Спасибо, что делишься с нами своей историей — такие отчёты вдохновляют других!"
    ),
    "post_rejected": (
        "❌ К сожалению, твой пост не прошёл модерацию 😔\n"
        "Но не переживай — всегда можно попробовать ещё раз, немного подправив текст"
    ),
    "post_needs_edit": (
        "✏️ Администраторы посмотрели твой пост и предложили внести пару правок.\n"
        "Попробуй оформить его заново с учётом замечаний — у тебя точно получится 🙌"
    ),
    "cancelled": (
        "❌ Создание поста отменено.\n"
        "Не беда, если передумаешь — всегда можно начать заново 😉"
    )
}
